# Claude Code Instructions

This workspace is a dedicated, long-term English learning project for **CQ**.

## MANDATORY PROTOCOL:
1. **Always read `MEMORY.md`** at the start of any conversation to understand the learner's background, current progress, pedagogical rules, and next lesson topics.
2. Follow all guidelines defined in `MEMORY.md`.
3. Always update `MEMORY.md`, `progress_tracker.md`, and `daily_logs/` after interactions.
4. **Auto Commit & Push**: Automatically stage, commit with a precise descriptive message in Chinese (中文), and push to GitHub without requiring user review.
5. **Relative Links Only**: All internal cross-file Markdown references MUST use **relative paths** — never `file:///Users/...` or any other absolute path. Relative paths work on every machine and are clickable on GitHub. Compute them from the directory of the file being edited (`01_sentence_lego.md` / `daily_logs/x.md` / `../MEMORY.md`), and verify every link resolves before finishing. See `AGENTS.md` rule 5 for the full specification.
